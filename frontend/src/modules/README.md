# Frontend Modules

按业务域创建 `ops`、`market-intelligence`、`demand-forecast`、`revenue-management`、`channel-operations`、`content-growth`、`reputation-membership`、`knowledge-measurement` 模块。每个模块包含 `views`、`components`、`api`、`store` 和 `types`。
